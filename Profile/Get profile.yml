info:
  name: Get profile
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/profile/me"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
