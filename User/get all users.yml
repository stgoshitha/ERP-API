info:
  name: get all users
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/users"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
