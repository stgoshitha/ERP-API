info:
  name: Update user status
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{base_url}}/users/4956ffa3-250e-4899-b1f3-31469d51d6b3/activate"
  body:
    type: json
    data: "{\r

      \    \"userStatus\" : \"INACTIVE\"\r

      }"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
