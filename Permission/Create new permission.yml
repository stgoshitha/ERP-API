info:
  name: Create new permission
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/permissions"
  body:
    type: json
    data: "{\r

      \  \"permissionName\": \"Employee Make Attendance\",\r

      \  \"description\": \"Allows create,read,update and delete Attendances\"\r

      }\r\n"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
