info:
  name: sign in
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/auth/login"
  body:
    type: json
    data: "// {\r

      //   \"email\":\"john.doe@example.com\",\r

      //  \"password\":\"Test@1234\"\r

      // }\r

      \r

      {\r

      \    \"email\": \"usererp@gmail.com\",\r

      \    \"password\": \"1qaz@Wsx\"\r

      }\r

      \r

      // {\r

      //  \"email\": \"goshitha.premachandra@test.com\",\r

      //   \"password\": \"Test@1234\"\r

      // }\r

      \r

      // {\r

      //     \"email\": \"ranaweeraimashi880@gmail.com\",\r

      //     \"password\": \"Test@2026\"\r

      // }"
  auth:
    type: bearer
    token: "{{bearerToken}}"

runtime:
  scripts:
    - type: after-response
      code: "if (res.getStatus() === 200) {\r

        \    const response = res.getBody();\r

        \    bru.setEnvVar(\"bearerToken\", response.data.token);\r

        }"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
