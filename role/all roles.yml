info:
  name: all roles
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/roles"
  body:
    type: json
    data: "{\r

      \    \"roleCode\": \"ROLE-001\"\r

      }"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
