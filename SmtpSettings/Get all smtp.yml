info:
  name: Get all smtp
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/smtp-settings"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
