info:
  name: Get smtp by purpose
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/smtp-settings/purpose/SYSTEM_NOTIFICATIONS"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
