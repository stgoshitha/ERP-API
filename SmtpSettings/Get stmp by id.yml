info:
  name: Get stmp by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/smtp-settings/f8cc9986-8a0c-49bf-b00d-2377b32d4546"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
